--[[
    FIQQZR7 FLING • MODERN DARK RED UI
    Multi-Target Fling Exploit dengan gaya FIQQZR7
]]

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Player = Players.LocalPlayer

-- GUI Setup
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FIQQZR7_FlingGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game:GetService("CoreGui")

-- Main Frame (lebih kecik)
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 260, 0, 320)  -- Tambah sikit untuk muat title bar
MainFrame.Position = UDim2.new(0.5, -130, 0.5, -160)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 8, 8)  -- Dark red base
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- Sudut modern
local MainCorner = Instance.new("UICorner", MainFrame)
MainCorner.CornerRadius = UDim.new(0, 12)

-- Glow effect (stroke luar merah gelap)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Thickness = 1.5
MainStroke.Color = Color3.fromRGB(180, 20, 20)  -- Dark red
MainStroke.Transparency = 0.3

-- Inner glow
local InnerStroke = Instance.new("UIStroke", MainFrame)
InnerStroke.Thickness = 0.5
InnerStroke.Color = Color3.fromRGB(255, 60, 60)
InnerStroke.Transparency = 0.6

-- Gradient gelap ke merah gelap
local Gradient = Instance.new("UIGradient", MainFrame)
Gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(25, 10, 10)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 5, 5))
})
Gradient.Rotation = 90

-- Title Bar (dikembalikan)
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 35)  -- Tinggi sikit biar nampak
TitleBar.BackgroundColor3 = Color3.fromRGB(20, 8, 8)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame

-- Title bar corner (hanya atas)
local TitleBarCorner = Instance.new("UICorner", TitleBar)
TitleBarCorner.CornerRadius = UDim.new(0, 12)

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -60, 1, 0)
Title.Position = UDim2.new(0, 12, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "🔥 FIQQZR7 FLING"
Title.TextColor3 = Color3.fromRGB(255, 40, 40)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = TitleBar

-- Close Button dengan glow
local CloseButton = Instance.new("TextButton")
CloseButton.Position = UDim2.new(1, -32, 0.5, -12)
CloseButton.Size = UDim2.fromOffset(24, 24)
CloseButton.BackgroundColor3 = Color3.fromRGB(180, 20, 20)
CloseButton.BorderSizePixel = 0
CloseButton.Text = "✕"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 16
CloseButton.Parent = TitleBar

local CloseCorner = Instance.new("UICorner", CloseButton)
CloseCorner.CornerRadius = UDim.new(0, 8)

local CloseStroke = Instance.new("UIStroke", CloseButton)
CloseStroke.Thickness = 1
CloseStroke.Color = Color3.fromRGB(255, 60, 60)
CloseStroke.Transparency = 0.3

-- Minimize Button (optional, untuk bagi seimbang)
local MinButton = Instance.new("TextButton")
MinButton.Position = UDim2.new(1, -60, 0.5, -12)
MinButton.Size = UDim2.fromOffset(24, 24)
MinButton.BackgroundColor3 = Color3.fromRGB(100, 20, 20)
MinButton.BorderSizePixel = 0
MinButton.Text = "—"
MinButton.TextColor3 = Color3.fromRGB(255, 200, 200)
MinButton.Font = Enum.Font.GothamBold
MinButton.TextSize = 16
MinButton.Parent = TitleBar

local MinCorner = Instance.new("UICorner", MinButton)
MinCorner.CornerRadius = UDim.new(0, 8)

local MinStroke = Instance.new("UIStroke", MinButton)
MinStroke.Thickness = 1
MinStroke.Color = Color3.fromRGB(180, 20, 20)
MinStroke.Transparency = 0.3

-- Status Label (turun sikit sebab title bar dah ada)
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Position = UDim2.new(0, 10, 0, 45)
StatusLabel.Size = UDim2.new(1, -20, 0, 22)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Pilih mangsa nak fling"
StatusLabel.TextColor3 = Color3.fromRGB(220, 180, 180)
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextSize = 14
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.Parent = MainFrame

-- Player Selection Frame
local SelectionFrame = Instance.new("Frame")
SelectionFrame.Position = UDim2.new(0, 10, 0, 72)
SelectionFrame.Size = UDim2.new(1, -20, 0, 160)
SelectionFrame.BackgroundColor3 = Color3.fromRGB(25, 12, 12)
SelectionFrame.BorderSizePixel = 0
SelectionFrame.Parent = MainFrame

local SelectionCorner = Instance.new("UICorner", SelectionFrame)
SelectionCorner.CornerRadius = UDim.new(0, 10)

local SelectionStroke = Instance.new("UIStroke", SelectionFrame)
SelectionStroke.Thickness = 1
SelectionStroke.Color = Color3.fromRGB(180, 20, 20)
SelectionStroke.Transparency = 0.4

-- Player List ScrollFrame
local PlayerScrollFrame = Instance.new("ScrollingFrame")
PlayerScrollFrame.Position = UDim2.new(0, 5, 0, 5)
PlayerScrollFrame.Size = UDim2.new(1, -10, 1, -10)
PlayerScrollFrame.BackgroundTransparency = 1
PlayerScrollFrame.BorderSizePixel = 0
PlayerScrollFrame.ScrollBarThickness = 4
PlayerScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(180, 20, 20)
PlayerScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
PlayerScrollFrame.Parent = SelectionFrame

-- Button Frame untuk susun button kemas
local ButtonFrame = Instance.new("Frame")
ButtonFrame.Position = UDim2.new(0, 10, 0, 240)
ButtonFrame.Size = UDim2.new(1, -20, 0, 65)
ButtonFrame.BackgroundTransparency = 1
ButtonFrame.Parent = MainFrame

-- Start Fling Button
local StartButton = Instance.new("TextButton")
StartButton.Position = UDim2.new(0, 0, 0, 0)
StartButton.Size = UDim2.new(0.48, -2, 0, 30)
StartButton.BackgroundColor3 = Color3.fromRGB(180, 20, 20)
StartButton.BorderSizePixel = 0
StartButton.Text = "🚀 START"
StartButton.TextColor3 = Color3.fromRGB(255, 255, 255)
StartButton.Font = Enum.Font.GothamBold
StartButton.TextSize = 14
StartButton.Parent = ButtonFrame

local StartCorner = Instance.new("UICorner", StartButton)
StartCorner.CornerRadius = UDim.new(0, 8)

local StartStroke = Instance.new("UIStroke", StartButton)
StartStroke.Thickness = 1
StartStroke.Color = Color3.fromRGB(255, 60, 60)
StartStroke.Transparency = 0.3

-- Stop Fling Button
local StopButton = Instance.new("TextButton")
StopButton.Position = UDim2.new(0.52, 2, 0, 0)
StopButton.Size = UDim2.new(0.48, -2, 0, 30)
StopButton.BackgroundColor3 = Color3.fromRGB(100, 10, 10)
StopButton.BorderSizePixel = 0
StopButton.Text = "⏹️ STOP"
StopButton.TextColor3 = Color3.fromRGB(255, 200, 200)
StopButton.Font = Enum.Font.GothamBold
StopButton.TextSize = 14
StopButton.Parent = ButtonFrame

local StopCorner = Instance.new("UICorner", StopButton)
StopCorner.CornerRadius = UDim.new(0, 8)

local StopStroke = Instance.new("UIStroke", StopButton)
StopStroke.Thickness = 1
StopStroke.Color = Color3.fromRGB(180, 20, 20)
StopStroke.Transparency = 0.3

-- Select/Deselect Buttons
local SelectAllButton = Instance.new("TextButton")
SelectAllButton.Position = UDim2.new(0, 0, 0, 35)
SelectAllButton.Size = UDim2.new(0.48, -2, 0, 26)
SelectAllButton.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
SelectAllButton.BorderSizePixel = 0
SelectAllButton.Text = "✅ PILIH ALL"
SelectAllButton.TextColor3 = Color3.fromRGB(220, 180, 180)
SelectAllButton.Font = Enum.Font.Gotham
SelectAllButton.TextSize = 12
SelectAllButton.Parent = ButtonFrame

local SelectCorner = Instance.new("UICorner", SelectAllButton)
SelectCorner.CornerRadius = UDim.new(0, 8)

local SelectStroke = Instance.new("UIStroke", SelectAllButton)
SelectStroke.Thickness = 1
SelectStroke.Color = Color3.fromRGB(180, 20, 20)
SelectStroke.Transparency = 0.4

local DeselectAllButton = Instance.new("TextButton")
DeselectAllButton.Position = UDim2.new(0.52, 2, 0, 35)
DeselectAllButton.Size = UDim2.new(0.48, -2, 0, 26)
DeselectAllButton.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
DeselectAllButton.BorderSizePixel = 0
DeselectAllButton.Text = "❌ HAPUS ALL"
DeselectAllButton.TextColor3 = Color3.fromRGB(220, 180, 180)
DeselectAllButton.Font = Enum.Font.Gotham
DeselectAllButton.TextSize = 12
DeselectAllButton.Parent = ButtonFrame

local DeselectCorner = Instance.new("UICorner", DeselectAllButton)
DeselectCorner.CornerRadius = UDim.new(0, 8)

local DeselectStroke = Instance.new("UIStroke", DeselectAllButton)
DeselectStroke.Thickness = 1
DeselectStroke.Color = Color3.fromRGB(180, 20, 20)
DeselectStroke.Transparency = 0.4

-- Footer line
local FooterLine = Instance.new("Frame")
FooterLine.Size = UDim2.new(0.9, 0, 0, 1)
FooterLine.Position = UDim2.new(0.05, 0, 1, -25)
FooterLine.BackgroundColor3 = Color3.fromRGB(180, 20, 20)
FooterLine.BackgroundTransparency = 0.4
FooterLine.Parent = MainFrame

local FooterCorner = Instance.new("UICorner", FooterLine)
FooterCorner.CornerRadius = UDim.new(1, 0)

-- Version text
local Version = Instance.new("TextLabel")
Version.Size = UDim2.new(1, 0, 0, 16)
Version.Position = UDim2.new(0, 0, 1, -22)
Version.Text = "FIQQZR7XCODE"
Version.Font = Enum.Font.Gotham
Version.TextSize = 8
Version.TextColor3 = Color3.fromRGB(140, 80, 80)
Version.BackgroundTransparency = 1
Version.TextTransparency = 0.3
Version.Parent = MainFrame

-- Minimize functionality
local minimized = false
local bodyObjects = {StatusLabel, SelectionFrame, ButtonFrame, FooterLine, Version}

MinButton.MouseButton1Click:Connect(function()
    minimized = not minimized
    for _, obj in ipairs(bodyObjects) do
        obj.Visible = not minimized
    end
    MainFrame.Size = minimized and UDim2.new(0, 260, 0, 45) or UDim2.new(0, 260, 0, 320)
end)

-- Variables
local SelectedTargets = {}
local PlayerCheckboxes = {}
local FlingActive = false
local FlingConnection = nil
getgenv().OldPos = nil
getgenv().FPDH = workspace.FallenPartsDestroyHeight

-- Function to update player list
local function RefreshPlayerList()
    -- Clear existing player entries
    for _, child in pairs(PlayerScrollFrame:GetChildren()) do
        child:Destroy()
    end
    PlayerCheckboxes = {}
    
    -- Get players and sort them
    local PlayerList = Players:GetPlayers()
    table.sort(PlayerList, function(a, b) return a.Name:lower() < b.Name:lower() end)
    
    -- Create entries for each player
    local yPosition = 5
    for _, player in ipairs(PlayerList) do
        if player ~= Player then -- Don't include yourself
            -- Create player entry frame
            local PlayerEntry = Instance.new("Frame")
            PlayerEntry.Size = UDim2.new(1, -10, 0, 28)
            PlayerEntry.Position = UDim2.new(0, 5, 0, yPosition)
            PlayerEntry.BackgroundColor3 = Color3.fromRGB(35, 15, 15)
            PlayerEntry.BorderSizePixel = 0
            PlayerEntry.Parent = PlayerScrollFrame
            
            local EntryCorner = Instance.new("UICorner", PlayerEntry)
            EntryCorner.CornerRadius = UDim.new(0, 6)
            
            -- Create checkbox
            local Checkbox = Instance.new("TextButton")
            Checkbox.Size = UDim2.fromOffset(20, 20)
            Checkbox.Position = UDim2.new(0, 4, 0.5, -10)
            Checkbox.BackgroundColor3 = Color3.fromRGB(50, 20, 20)
            Checkbox.BorderSizePixel = 0
            Checkbox.Text = ""
            Checkbox.Parent = PlayerEntry
            
            local CheckCorner = Instance.new("UICorner", Checkbox)
            CheckCorner.CornerRadius = UDim.new(0, 4)
            
            -- Checkmark
            local Checkmark = Instance.new("TextLabel")
            Checkmark.Size = UDim2.new(1, 0, 1, 0)
            Checkmark.BackgroundTransparency = 1
            Checkmark.Text = "✓"
            Checkmark.TextColor3 = Color3.fromRGB(255, 40, 40)
            Checkmark.TextSize = 16
            Checkmark.Font = Enum.Font.GothamBold
            Checkmark.Visible = SelectedTargets[player.Name] ~= nil
            Checkmark.Parent = Checkbox
            
            -- Player name
            local NameLabel = Instance.new("TextLabel")
            NameLabel.Size = UDim2.new(1, -30, 1, 0)
            NameLabel.Position = UDim2.new(0, 28, 0, 0)
            NameLabel.BackgroundTransparency = 1
            NameLabel.Text = player.Name
            NameLabel.TextColor3 = Color3.fromRGB(240, 220, 220)
            NameLabel.TextSize = 13
            NameLabel.Font = Enum.Font.Gotham
            NameLabel.TextXAlignment = Enum.TextXAlignment.Left
            NameLabel.Parent = PlayerEntry
            
            -- Click area
            local ClickArea = Instance.new("TextButton")
            ClickArea.Size = UDim2.new(1, 0, 1, 0)
            ClickArea.BackgroundTransparency = 1
            ClickArea.Text = ""
            ClickArea.ZIndex = 2
            ClickArea.Parent = PlayerEntry
            
            ClickArea.MouseButton1Click:Connect(function()
                if SelectedTargets[player.Name] then
                    SelectedTargets[player.Name] = nil
                    Checkmark.Visible = false
                else
                    SelectedTargets[player.Name] = player
                    Checkmark.Visible = true
                end
                UpdateStatus()
            end)
            
            PlayerCheckboxes[player.Name] = {
                Entry = PlayerEntry,
                Checkmark = Checkmark
            }
            
            yPosition = yPosition + 32
        end
    end
    
    PlayerScrollFrame.CanvasSize = UDim2.new(0, 0, 0, yPosition + 5)
end

-- Count selected targets
local function CountSelectedTargets()
    local count = 0
    for _ in pairs(SelectedTargets) do
        count = count + 1
    end
    return count
end

-- Update status
local function UpdateStatus()
    local count = CountSelectedTargets()
    if FlingActive then
        StatusLabel.Text = "🔥 Fling " .. count .. " target(s)"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 60, 60)
    else
        StatusLabel.Text = "📌 " .. count .. " target(s) dipilih"
        StatusLabel.TextColor3 = Color3.fromRGB(220, 180, 180)
    end
end

-- Select/Deselect all
local function ToggleAllPlayers(select)
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= Player then
            local checkboxData = PlayerCheckboxes[player.Name]
            if checkboxData then
                if select then
                    SelectedTargets[player.Name] = player
                    checkboxData.Checkmark.Visible = true
                else
                    SelectedTargets[player.Name] = nil
                    checkboxData.Checkmark.Visible = false
                end
            end
        end
    end
    UpdateStatus()
end

-- Notification
local function Message(Title, Text, Time)
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = Title,
        Text = Text,
        Duration = Time or 5
    })
end

-- Fling function (sama macam asal)
local function SkidFling(TargetPlayer)
    local Character = Player.Character
    local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
    local RootPart = Humanoid and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not TCharacter then return end
    
    local THumanoid
    local TRootPart
    local THead
    local Accessory
    local Handle
    if TCharacter:FindFirstChildOfClass("Humanoid") then
        THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
    end
    if THumanoid and THumanoid.RootPart then
        TRootPart = THumanoid.RootPart
    end
    if TCharacter:FindFirstChild("Head") then
        THead = TCharacter.Head
    end
    if TCharacter:FindFirstChildOfClass("Accessory") then
        Accessory = TCharacter:FindFirstChildOfClass("Accessory")
    end
    if Accessory and Accessory:FindFirstChild("Handle") then
        Handle = Accessory.Handle
    end
    if Character and Humanoid and RootPart then
        if RootPart.Velocity.Magnitude < 50 then
            getgenv().OldPos = RootPart.CFrame
        end
        
        if THumanoid and THumanoid.Sit then
            return Message("Error", TargetPlayer.Name .. " is sitting", 2)
        end
        
        if THead then
            workspace.CurrentCamera.CameraSubject = THead
        elseif Handle then
            workspace.CurrentCamera.CameraSubject = Handle
        elseif THumanoid and TRootPart then
            workspace.CurrentCamera.CameraSubject = THumanoid
        end
        
        if not TCharacter:FindFirstChildWhichIsA("BasePart") then
            return
        end
        
        local FPos = function(BasePart, Pos, Ang)
            RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
            Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
            RootPart.Velocity = Vector3.new(9e7, 9e7 * 10, 9e7)
            RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        end
        
        local SFBasePart = function(BasePart)
            local TimeToWait = 2
            local Time = tick()
            local Angle = 0
            repeat
                if RootPart and THumanoid then
                    if BasePart.Velocity.Magnitude < 50 then
                        Angle = Angle + 100
                        FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle),0 ,0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle),0 ,0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle),0 ,0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle), 0, 0))
                        task.wait()
                    else
                        FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, -THumanoid.WalkSpeed), CFrame.Angles(0, 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0))
                        task.wait()
                    end
                end
            until Time + TimeToWait < tick() or not FlingActive
        end
        
        workspace.FallenPartsDestroyHeight = 0/0
        
        local BV = Instance.new("BodyVelocity")
        BV.Parent = RootPart
        BV.Velocity = Vector3.new(0, 0, 0)
        BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
        
        if TRootPart then
            SFBasePart(TRootPart)
        elseif THead then
            SFBasePart(THead)
        elseif Handle then
            SFBasePart(Handle)
        else
            return Message("Error", TargetPlayer.Name .. " has no valid parts", 2)
        end
        
        BV:Destroy()
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        workspace.CurrentCamera.CameraSubject = Humanoid
        
        -- Reset character position
        if getgenv().OldPos then
            repeat
                RootPart.CFrame = getgenv().OldPos * CFrame.new(0, .5, 0)
                Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0, .5, 0))
                Humanoid:ChangeState("GettingUp")
                for _, part in pairs(Character:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.Velocity, part.RotVelocity = Vector3.new(), Vector3.new()
                    end
                end
                task.wait()
            until (RootPart.Position - getgenv().OldPos.p).Magnitude < 25
            workspace.FallenPartsDestroyHeight = getgenv().FPDH
        end
    else
        return Message("Error", "Your character is not ready", 2)
    end
end

-- Start fling
local function StartFling()
    if FlingActive then return end
    
    local count = CountSelectedTargets()
    if count == 0 then
        StatusLabel.Text = "⚠️ Pilih target dulu!"
        wait(1)
        UpdateStatus()
        return
    end
    
    FlingActive = true
    UpdateStatus()
    Message("🔥 FIQQZR7 FLING", "Fling " .. count .. " target", 2)
    
    spawn(function()
        while FlingActive do
            local validTargets = {}
            
            for name, player in pairs(SelectedTargets) do
                if player and player.Parent then
                    validTargets[name] = player
                else
                    SelectedTargets[name] = nil
                    local checkbox = PlayerCheckboxes[name]
                    if checkbox then
                        checkbox.Checkmark.Visible = false
                    end
                end
            end
            
            for _, player in pairs(validTargets) do
                if FlingActive then
                    SkidFling(player)
                    wait(0.1)
                else
                    break
                end
            end
            
            UpdateStatus()
            wait(0.5)
        end
    end)
end

-- Stop fling
local function StopFling()
    if not FlingActive then return end
    FlingActive = false
    UpdateStatus()
    Message("⏹️ FIQQZR7 FLING", "Fling stopped", 2)
end

-- Button connections
StartButton.MouseButton1Click:Connect(StartFling)
StopButton.MouseButton1Click:Connect(StopFling)
SelectAllButton.MouseButton1Click:Connect(function() ToggleAllPlayers(true) end)
DeselectAllButton.MouseButton1Click:Connect(function() ToggleAllPlayers(false) end)
CloseButton.MouseButton1Click:Connect(function()
    StopFling()
    ScreenGui:Destroy()
end)

-- Player events
Players.PlayerAdded:Connect(RefreshPlayerList)
Players.PlayerRemoving:Connect(function(player)
    if SelectedTargets[player.Name] then
        SelectedTargets[player.Name] = nil
    end
    RefreshPlayerList()
    UpdateStatus()
end)

-- Initialize
RefreshPlayerList()
UpdateStatus()

Message("✅ FIQQZR7 FLING", "Dark Red Edition siap!", 3)
